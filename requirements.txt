# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database — PostgreSQL (production) with SQLite fallback (development/testing)
# psycopg 3 provides the binary extension for fast PostgreSQL connectivity
# The app falls back to built-in sqlite3 when DATABASE_URL is not set.
psycopg[binary]==3.3.4
python-dotenv==1.1.0

# Scheduling
apscheduler==3.11.0

# HTTP Client
httpx==0.28.1

# CORS for frontend
# (included with fastapi)

# Email
# (smtplib is built-in)

# Templating (for dashboard)
jinja2==3.1.5

# Date handling
python-dateutil==2.9.0
pytz==2024.2

# Caching & state
# (json + sqlite are built-in)

# WebSocket (for live market data — eliminates REST polling for prices/klines)
websocket-client==1.9.0

# Web serving
python-multipart==0.0.20
